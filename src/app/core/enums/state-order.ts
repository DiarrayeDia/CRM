export enum StateOrder {
  CANCELED = 'CANCELED',
  OPTION = 'OPTION',
  CONFIRMED = 'CONFIRMED',
}
// StateOrder.CANCELED => CANCELED

export enum StateClient {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}
